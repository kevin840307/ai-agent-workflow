# Common Workflow Markdown

Reusable global markdown assets live here. Any workflow can reference them with `steps/common/<file>.md`. Project-local `.ai-workflow/steps/common/<file>.md` overrides the global version.
