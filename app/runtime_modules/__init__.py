"""Focused runtime modules used by the workflow runtime.

New code should import from ``app.runtime_modules.<module>`` directly.
Legacy ``app/runtime*.py`` files are intentionally empty deletion markers in
patches so they can be removed by downstream consumers.
"""
